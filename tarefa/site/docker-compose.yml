services:
  web:
    build: .
    container_name: pipeline-docker-aula
    ports:
      - "5000:5000"
    volumes:
      - .:/app          # Permite hot-reload durante o desenvolvimento
    environment:
      - FLASK_ENV=development
    restart: unless-stopped