services:
  web:
    build: .
    container_name: site
    ports:
      - "5000:5000"
    volumes:
      - .:/app          # Permite hot-reload durante o desenvolvimento
    environment:
      - FLASK_ENV=development
    restart: unless-stopped