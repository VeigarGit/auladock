[project]
name = "cadastro-usuarios"
version = "0.1.0"
description = "Sistema de cadastro de usuarios com Flask e MySQL"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "pymysql>=1.1",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
